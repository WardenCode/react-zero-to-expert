const active = true;

// let msg = "";

// if ( active ) {
// 	msg = "Active";
// } else {
// 	msg = "Inactive";
// }

// Ternario normal
// let msg = ( active ) ? "Active" : "Inactive";

// Ternario de solo if
let msg = active && "Active";

console.log( msg );
